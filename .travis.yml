language: python
python:
    - "3.7"
env:
    - DJANGO_VERSION==3.0.4
before_install:
    - sudo apt-get install python3-pip
install:
    - sudo pip3 install -q Django=$DJANGO_VERSION
    - sudo pip3 install -q -r requirements.txt
before_script: 
    - cd AjoudongBE
script:
- python manage.py test

language: android
jdk: oraclejdk8
android:
    components:
        - tools
        - android-27
        - build-tools-29.0.3
before_script:
    - cd ..
    - cd AjouDongFE
script:
    - ./gradlew build connectedCheck

notifications:
    slack: swcapposturn:qW81nIoC5VXTF6TgjIqqE8bG
